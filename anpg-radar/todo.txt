version 1:
* improve handling geofences… (make region for the current list, trigger when leaving region…
* expiration timestamps of saved list 
* make 2nd page

* colors/styling
i18n
* “backend”
* google analytics
handle notification subscription (state of switch, disable witch untill success block called)

version 2:
style notifications
make “ignore” button on certain cameras… / make custom action for that + mute button
download images locally, use in notifications

* center map on current position
* handle permissions for notfications and location
* fix dragging map
* memory leak 

// notifications not working on lock screen


* use UNLocationNotificationTrigger instead?
